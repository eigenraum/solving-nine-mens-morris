{
  "name": "nmm-web",
  "version": "0.1.0",
  "private": true,
  "description": "In-browser Nine Men's Morris engine: rules + neural evaluator, no server.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "verify-fixtures": "npm run build && node dist/verify_fixtures.js fixtures",
    "verify-golden": "npm run build && node dist/verify_golden.js export"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.5.0"
  }
}
